# 内容贡献指南

## 新增或修改条目

1. 使用 `npm run content:add -- ...` 创建条目，由工具生成全仓库唯一的 UUID v4。
2. 折叠状态可读的文字写入 `summary`；仅在确有补充信息时增加 `body`。
3. 选择与内容语义对应的 `kind`，并添加少量可检索标签。
4. 外部事实或引用尽量填写 HTTPS `sourceUrl`，确认许可后再提交。
5. 已有 ID 在改写内容或移动分类时保持不变，构建过程也不会生成 ID。
6. 运行 `npm run validate`；提交前可运行 `npm run build` 检查发布形态。

## 修改资料库

新增资料库时必须同时：

1. 在 `catalog.json` 中注册元数据和唯一排序值。
2. 新建同名 `sources/<sourceId>.json`。
3. 保持 `sourceId` 与文件名一致。
4. 与 Breakpeek 插件确认默认选择和旧配置兼容策略。

删除或重命名资料库属于破坏性变更，应先在插件端支持迁移，不应直接合并。

## 审查重点

- 内容准确、简洁，不包含攻击性、歧视性或隐私信息。
- 面试题的 `summary` 不提前泄露完整答案。
- 不包含原始 HTML、脚本 URL、模型提示词或插件配置指令。
- 时效性信息设置合理的发布时间和必要的过期时间。
- `id` 不因措辞、顺序或文件位置变化而改变。
